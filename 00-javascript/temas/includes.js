// includes: verifica se o valor de tipo está incluso no array, retornando um boolean. Não usar com objetos
const numeros = [1, 2, 3]

console.log(numeros.includes(1))
console.log(numeros.includes(0))
